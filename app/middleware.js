
export function middleware(req, res) {
    // if (req.nextUrl.pathname.startsWith('http://localhost:3000/api/header-authorization')){

    // let requestHeader = new Headers(req.headers);
    // requestHeader.set
    // }

    console.log("I am middleware")
}